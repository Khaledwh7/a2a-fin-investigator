[pytest]
asyncio_mode = auto
testpaths = tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
    # Some unit tests use deliberately short JWT secrets for brevity.
    ignore:.*HMAC key.*:UserWarning
